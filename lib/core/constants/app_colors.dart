import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - minimal and distraction-free
  static const Color primary = Color(0xFF4A90E2);
  static const Color primaryDark = Color(0xFF357ABD);
  static const Color primaryLight = Color(0xFF6BA3E5);

  // Background colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Accent colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);

  // Focus mode colors
  static const Color focusActive = Color(0xFF00BCD4);
  static const Color focusBreak = Color(0xFFFFEB3B);

  // Overlay colors
  static const Color overlay = Color(0x8A000000);
  static const Color divider = Color(0xFFE0E0E0);
}
